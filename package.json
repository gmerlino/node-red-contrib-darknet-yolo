{
  "name": "@smartme.io/node-red-contrib-darknet-yolo",
  "version": "0.0.4",
  "description": "A simple node that detects objects in an image using Darknet YOLO",
  "main": "yolo-detect.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Francesco Longo",
  "license": "GPL-2.0",
  "dependencies": {
    "@moovel/yolo": "0.2.0"
  },
  "keywords": [ "node-red", "node", "darknet", "yolo" ],
  "node-red": {
    "nodes": {
      "yolo-detect": "yolo-detect.js"
    }
  }
}
